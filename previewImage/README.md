# previewImage.js

Простой jQuery-плагин для создания красивого загрузчика изображений с превью, кнопкой удаления и валидацией.

<!-- ![Пример](screenshot.png) если добавишь скрин -->

## 🔧 Особенности

- Автоматически создаёт форму с превью
- Поддержка drag & drop (в будущем)
- Кнопка "×" для удаления изображения
- Валидация: тип и размер файла
- Кастомизация через опции
- Лёгкий и готов к использованию

## 📦 Установка

1. Подключи jQuery и `previewImage.js`:

```html
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="previewImage.js"></script>

<script>
  $("#myUploader").previewImage({
    label: "Фото новости (872×600px)",
    buttonText: "Загрузить фото",
    defaultImage: "/assets/img/icon_empty-img.svg",
    maxSizeMB: 3,
    onSelect: function (file) {
      console.log("Файл загружен:", file.name);
    },
    onError: function (message) {
      alert("Ошибка: " + message);
    },
  });
</script>
```

## 📁 Структура

```html
<div id="myUploader"></div>
<!-- в него вставится всё -->
```

## 💡 Пример

Открой demo.html в браузере, чтобы посмотреть, как работает.

## 🤝 Автор

@Samjuk
